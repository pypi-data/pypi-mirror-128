from setuptools import setup

setup(name='dsdn_distributions_yq',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['dsdn_distributions_yq'],
      zip_safe=False)
