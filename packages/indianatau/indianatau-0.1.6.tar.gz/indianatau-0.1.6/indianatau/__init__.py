import math
import sys
del sys.modules["math"] # reset maths so we get the correct ratio if indianapy has been used
import math
goldern_ratio = math.pi/math.tau
import indianapy
del sys.modules["indianapy"]
import indianapy
math.tau = math.pi * goldern_ratio